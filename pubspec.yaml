name: lan_scanner
description: Dart / Flutter package that allows discovering network devices via multi-threaded ICMP pings.
version: 4.0.0+1
homepage: https://github.com/ivirtex/lan_scanner

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  dart_ping: ^9.0.0

dev_dependencies:
  import_sorter: ^4.6.0
  test: ^1.24.5
  very_good_analysis: ^5.0.0+1
